<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content">

    <!-- Step Icon Circle Indicator -->
    <FrameLayout
        android:id="@+id/layoutStepIndicator"
        android:layout_width="32dp"
        android:layout_height="32dp"
        android:background="@drawable/bg_badge_add_green"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <ImageView
            android:id="@+id/ivStepStatusIcon"
            android:layout_width="16dp"
            android:layout_height="16dp"
            android:layout_gravity="center"
            android:src="@drawable/ic_check_circle"
            app:tint="@color/white" />
    </FrameLayout>

    <!-- Vertical Timeline Connecting Line -->
    <View
        android:id="@+id/viewTimelineLine"
        android:layout_width="2dp"
        android:layout_height="0dp"
        android:background="@color/border_light"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="@+id/layoutStepIndicator"
        app:layout_constraintStart_toStartOf="@+id/layoutStepIndicator"
        app:layout_constraintTop_toBottomOf="@+id/layoutStepIndicator" />

    <!-- Step Title -->
    <TextView
        android:id="@+id/tvStepTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginEnd="8dp"
        android:fontFamily="sans-serif-bold"
        android:textColor="@color/text_primary"
        android:textSize="14sp"
        app:layout_constraintEnd_toStartOf="@+id/tvStepTimestamp"
        app:layout_constraintStart_toEndOf="@+id/layoutStepIndicator"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="Kitchen Preparing" />

    <!-- Step Description -->
    <TextView
        android:id="@+id/tvStepDesc"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="14dp"
        android:layout_marginTop="2dp"
        android:layout_marginEnd="8dp"
        android:layout_marginBottom="20dp"
        android:textColor="@color/text_secondary"
        android:textSize="12sp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@+id/tvStepTimestamp"
        app:layout_constraintStart_toEndOf="@+id/layoutStepIndicator"
        app:layout_constraintTop_toBottomOf="@+id/tvStepTitle"
        tools:text="Chef is grilling patty and assembling fresh buns" />

    <!-- Step Timestamp -->
    <TextView
        android:id="@+id/tvStepTimestamp"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textColor="@color/text_hint"
        android:textSize="12sp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="06:22 PM" />

</androidx.constraintlayout.widget.ConstraintLayout>
